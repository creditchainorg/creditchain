import Summary from './SUMMARY.mdx';

# CLI Reference

The CreditChain node is operated via the CLI by running the `creditchaind node` command. To stop it, press `ctrl-c`. You may need to wait a bit as CreditChain tears down existing p2p connections or performs other cleanup tasks.

However, CreditChain has more commands:

<Summary />
